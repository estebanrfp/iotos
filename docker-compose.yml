app:
  build: ./
  command: nodemon
  restart: always
  privileged: true
  env_file:
    - ./env.sh
  links:
    - auto
auto:
  build: ./
  command: nodemon --watch . auto.js
  restart: always
  privileged: true
  env_file:
    - ./env.sh
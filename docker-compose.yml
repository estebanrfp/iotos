app:
  build: ./
  command: nodemon
  restart: always
  privileged: true
  volumes:
    - .:/data
  env_file:
    - ./env.sh
auto:
  build: ./
  command: nodemon --watch . auto.js
  restart: always
  privileged: true
  links:
    - data
  env_file:
    - ./env.sh